# `config.py` raise-site coverage

Generated by `scripts/gen-golden-corpus.py`. Each site is keyed by
`<qualname>#<n>` — the n-th `raise` inside that function — and
labelled with its message TEMPLATE, every interpolation collapsed to
`{…}`.

Neither the key nor the label is round-tripped source, and no line
numbers appear here. That is deliberate twice over: this report is a
committed fixture, so it must not churn on edits elsewhere in the
file, and it must be byte-identical on every CPython in the CI
matrix. `ast.unparse` is neither — PEP 701 moved its f-string quote
selection — so nothing here goes through it. See the corpus README,
“Interpreter independence”.

- total raise sites: 91
- reached by the corpus: 80
- not reached: 11

## Reached

- `validate_transform#0` — `ValueError: unknown secret_injection transform: '{…}'. Valid: {…}`
- `_host_dns_servers#0` — `` RuntimeError: Could not detect usable DNS servers: /etc/resolv.conf contains only loopback addresses and `scutil --dns` reported no usable upstream resolvers. Set dns_servers explicitly in your age... ``
- `_host_dns_servers#1` — `RuntimeError: Could not detect usable DNS servers: /etc/resolv.conf contains only loopback addresses (e.g. 127.0.0.53 from systemd-resolved) and {…} is missing or empty. Set dns_servers explicitly ...`
- `_agent_mapping#0` — `ValueError: {…} must be a mapping (got {…})`
- `validate_agents_raw#0` — `ValueError: config must be a mapping`
- `validate_agents_raw#1` — `ValueError: domains.auto is no longer supported; use agents.decider with flat LLM fields`
- `validate_agents_raw#2` — `ValueError: top-level watcher is no longer supported; use agents.watcher with flat LLM fields`
- `validate_agents_raw#3` — `ValueError: unknown agents: {…}`
- `validate_agents_raw#4` — `ValueError: {…}.kind is no longer supported; omit kind`
- `validate_agents_raw#5` — `ValueError: {…}: LLM fields must be flat, not under '{…}'`
- `validate_agents_raw#6` — `ValueError: {…}.{…} must be a boolean (true/false)`
- `load_config#0` — `ValueError: {…} is not valid YAML{…}: {…}`
- `load_config#1` — `ValueError: could not read {…}: {…}`
- `load_config#2` — `ValueError: invalid secrets.scope: {…}. Valid: {…}`
- `load_config#3` — `ValueError: invalid secrets.backend: {…}. Valid: {…}`
- `load_config._llm_client#0` — `ValueError: {…} must be a number, not a boolean`
- `load_config._llm_client#1` — `ValueError: {…}: invalid LLM client value ({…})`
- `load_config#7` — `ValueError: agents.decider.context must be a string (got {…})`
- `load_config#8` — `ValueError: agents.decider.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {…}`
- `load_config#11` — `ValueError: agents.watcher.context must be a string (got {…})`
- `load_config._w_num#0` — `ValueError: agents.watcher.{…} must be a number (got {…})`
- `load_config#14` — `ValueError: agents.watcher.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {…}`
- `load_config#15` — `ValueError: ports must be a mapping with 'tcp', 'udp', and/or 'icmp' keys (got: {…})`
- `load_config#16` — `ValueError: ports.tcp must be a mapping with 'allow' and/or 'passthrough' keys (got: {…}). If you meant to list TCP ports, use 'ports.tcp.allow: [...]'`
- `load_config#17` — `ValueError: ports.tcp.allow must be a list of integers (got: {…})`
- `load_config#18` — `ValueError: ports.tcp.passthrough must be a list of integers (got: {…})`
- `load_config#19` — `ValueError: ports.udp must be a mapping with an 'allow' key (got: {…}). If you meant to list UDP ports, use 'ports.udp.allow: [...]'`
- `load_config#20` — `ValueError: ports.udp.allow must be a list of integers (got: {…})`
- `load_config#21` — `ValueError: ports.icmp must be a mapping with an 'allow' boolean (got: {…}). To allow outbound ping, use 'ports.icmp.allow: true'`
- `load_config#22` — `ValueError: ports.icmp.allow must be a boolean (got: {…})`
- `_validate_agent_max_tokens#1` — `ValueError: {…} must be at least {…} (got {…}) — a reasoning model spends thinking tokens inside this budget before emitting the forced tool call, so a smaller ceiling returns finish_reason=length ...`
- `validate_config#0` — `ValueError: 'name' is required in config`
- `validate_config#1` — `ValueError: 'name' must be 1-63 lowercase alphanumeric characters or hyphens, starting with a letter or digit (got: {…})`
- `validate_config#2` — `ValueError: container.image is required in config`
- `validate_config#3` — `ValueError: invalid container image reference: {…}`
- `validate_config#4` — `ValueError: isolation must be 'container', 'vm', or 'apple-container' (got: {…})`
- `validate_config#5` — `ValueError: lifecycle must be one of {…} (got: {…})`
- `validate_config#6` — `ValueError: container isolation is not available on macOS; use vm or apple-container instead`
- `validate_config#7` — `ValueError: apple-container isolation requires macOS; current platform is {…}`
- `validate_config#8` — `ValueError: apple-container isolation requires Apple Silicon (arm64); current arch is {…}`
- `validate_config#9` — `ValueError: vm.vcpus must be >= 1`
- `validate_config#10` — `ValueError: vm.mem_mb must be >= 128`
- `validate_config#11` — `ValueError: logging.level must be one of {…} (got: {…})`
- `validate_config#12` — `ValueError: logging.{…} must be one of {…} or empty (got: {…})`
- `validate_config#13` — `ValueError: invalid port spec {…}: expected HOST_PORT:CONTAINER_PORT or BIND:HOST_PORT:CONTAINER_PORT`
- `validate_config#14` — `ValueError: invalid port number {…} in port spec {…}`
- `validate_config#15` — `ValueError: port {…} out of range (1-65535) in port spec {…}`
- `validate_config._check_port_entry#0` — `ValueError: {…} entries must be integers (got: {…})`
- `validate_config._check_port_entry#1` — `ValueError: {…} entry {…} out of range (1-65535)`
- `validate_config._validate_port_list#0` — `ValueError: {…} entry {…} appears more than once`
- `validate_config#16` — `ValueError: ports.tcp.allow entry {…} is reserved by mitmdump (8080 = HTTP-proxy listener, 8443 = transparent listener); redirecting it would loop or break the L7 proxy path. Move it to ports.tcp.p...`
- `validate_config#17` — `ValueError: ports.tcp.allow entry {…} collides with protocol_relays[{…}].listen={…}; the REDIRECT would intercept connections meant for the relay. Move it to ports.tcp.passthrough if the cage also ...`
- `validate_config#18` — `ValueError: ports.tcp.allow entry {…} collides with container.ports inbound forward {…}; the REDIRECT would intercept connections meant for the cage's reverse-mode listener`
- `validate_config#19` — `ValueError: domains: cannot specify both 'allow' and 'block' lists`
- `validate_config#20` — `ValueError: invalid domain syntax: {…} — expected a plain lowercase hostname (e.g. 'api.example.com', or a bare LAN name like 'fcos-vm-home-01')`
- `validate_config#21` — `ValueError: nested_containers is not supported with vm isolation`
- `validate_config#22` — `ValueError: agents.{…}.timeout_seconds must be finite and > 0`
- `validate_config#23` — `ValueError: agents.decider.host {…} must be a dotted hostname (e.g. 'agentcage.local'), not an IP literal or single label`
- `validate_config#24` — `ValueError: agents.decider.host {…} must not appear in domains.allow/block/passthrough — the control host is a synthetic, non-forwardable endpoint`
- `validate_config#25` — `ValueError: agents.decider requires domains allowlist mode (a grant only widens an allowlist; in blocklist mode everything not blocked is already reachable).`
- `validate_config#26` — `ValueError: agents.decider.provider must be 'anthropic', 'openai', or 'openrouter' (got {…})`
- `validate_config#27` — `ValueError: agents.decider.model is required`
- `validate_config#28` — `ValueError: agents.decider.api_key is required — the decider agent needs its own API key, an egress-only secret using the source: scheme (e.g. 'systemd-creds:POLICY_LLM_KEY' or 'env:OPENROUTER_API_...`
- `validate_config#30` — `ValueError: agents.decider.api_key does not support cmd: sources (the egress container has no shell); use env:NAME or systemd-creds:NAME`
- `validate_config#31` — `ValueError: agents.decider.api_key unknown source scheme: '{…}'. Valid schemes: env, systemd-creds`
- `validate_config#32` — `ValueError: agents.decider.base_url must be an https:// URL (the decider API key is sent on every call; http:// would leak it in cleartext — got {…})`
- `validate_config#33` — `ValueError: agents.decider.rate_limit requests_per_second/burst must be >= 0`
- `validate_config#34` — `ValueError: agents.decider.context is too long ({…} chars, max 4096) — trim it or move details into a shorter summary`
- `validate_config#36` — `ValueError: agents.watcher does not support domains blocklist mode (there the static baseline IS the block list, so the watcher's digest and its baseline recommendations invert — a recommended remo...`
- `validate_config#37` — `ValueError: agents.watcher.provider must be 'anthropic', 'openai', or 'openrouter' (got {…})`
- `validate_config#38` — `ValueError: agents.watcher.model is required`
- `validate_config#39` — `ValueError: agents.watcher.api_key is required — the watcher agent needs its own API key, an egress-only secret using the source: scheme (e.g. 'systemd-creds:WATCHER_LLM_KEY' or 'env:WATCHER_LLM_KE...`
- `validate_config#41` — `ValueError: agents.watcher.api_key does not support cmd: sources (the egress container has no shell); use env:NAME or systemd-creds:NAME`
- `validate_config#42` — `ValueError: agents.watcher.api_key unknown source scheme: '{…}'. Valid schemes: env, systemd-creds`
- `validate_config#43` — `ValueError: agents.watcher.base_url must be an https:// URL (the watcher API key is sent on every call; http:// would leak it in cleartext — got {…})`
- `validate_config#44` — `ValueError: agents.watcher.interval_seconds must be >= 60 (got {…}) — one LLM scan per interval, and a faster cadence would be a hot loop`
- `validate_config#45` — `ValueError: agents.watcher.window_seconds must be in (0, 86400] (got {…})`
- `validate_config#46` — `ValueError: agents.watcher.max_flows must be in [10, 2000] (got {…})`
- `validate_config#47` — `ValueError: agents.watcher.max_digest_tokens must be 0 (unbounded) or in [2000, 500000] (got {…})`
- `validate_config#48` — `ValueError: agents.watcher.context is too long ({…} chars, max 4096) — trim it or move details into a shorter summary`

## Not reached

- `load_config#4` — `ValueError: agents must be a mapping (got {…})`
- `load_config#5` — `ValueError: agents.decider must be a mapping (got {…})`
- `load_config#6` — `ValueError: agents.decider.enable must be a boolean (true/false) — got {…}`
- `load_config#9` — `ValueError: agents.watcher must be a mapping (got {…})`
- `load_config#10` — `ValueError: agents.watcher.enable must be a boolean (true/false) — got {…}`
- `load_config#12` — `ValueError: agents.watcher.auto_revoke must be a boolean (true/false) — got {…}`
- `load_config#13` — `ValueError: agents.watcher.dedup_samples must be a boolean (true/false) — got {…}`
- `_validate_agent_max_tokens#0` — `ValueError: {…} must be an integer (got {…})`
- `validate_config#29` — `ValueError: agents.decider.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {…}`
- `validate_config#35` — `ValueError: agents.decider.host must always be in never_grant (internal invariant violated)`
- `validate_config#40` — `ValueError: agents.watcher.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {…}`
