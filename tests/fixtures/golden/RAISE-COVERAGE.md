# `config.py` raise-site coverage

Generated by `scripts/gen-golden-corpus.py`. Each site is keyed by
`<qualname>#<n>` — the n-th `raise` inside that function. No line
numbers appear here on purpose: the key and the raised expression are
both stable under edits elsewhere in the file, so this report only
changes when a raise site is genuinely added, removed, or moved.

- total raise sites: 91
- reached by the corpus: 80
- not reached: 11

## Reached

- `validate_transform#0` — `ValueError(f"unknown secret_injection transform: '{name}'. Valid: {valid}")`
- `_host_dns_servers#0` — `RuntimeError('Could not detect usable DNS servers: /etc/resolv.conf contains only loopback addresses and `scutil --dns` reported no usable upstream resolvers...`
- `_host_dns_servers#1` — `RuntimeError(f'Could not detect usable DNS servers: /etc/resolv.conf contains only loopback addresses (e.g. 127.0.0.53 from systemd-resolved) and {_RESOLVED_...`
- `_agent_mapping#0` — `ValueError(f'{path} must be a mapping (got {type(value).__name__})')`
- `validate_agents_raw#0` — `ValueError('config must be a mapping')`
- `validate_agents_raw#1` — `ValueError('domains.auto is no longer supported; use agents.decider with flat LLM fields')`
- `validate_agents_raw#2` — `ValueError('top-level watcher is no longer supported; use agents.watcher with flat LLM fields')`
- `validate_agents_raw#3` — `ValueError(f"unknown agents: {', '.join(sorted(map(str, unknown)))}")`
- `validate_agents_raw#4` — `ValueError(f'{path}.kind is no longer supported; omit kind')`
- `validate_agents_raw#5` — `ValueError(f"{path}: LLM fields must be flat, not under '{wrapper}'")`
- `validate_agents_raw#6` — `ValueError(f'{path}.{flag} must be a boolean (true/false)')`
- `load_config#0` — `ValueError(f'{path} is not valid YAML{where}: {msg}')`
- `load_config#1` — `ValueError(f'could not read {path}: {e}')`
- `load_config#2` — `ValueError(f'invalid secrets.scope: {scope!r}. Valid: {valid}')`
- `load_config#3` — `ValueError(f'invalid secrets.backend: {backend!r}. Valid: {valid}')`
- `load_config._llm_client#0` — `ValueError(f'{key} must be a number, not a boolean')`
- `load_config._llm_client#1` — `ValueError(f'{label}: invalid LLM client value ({e})')`
- `load_config#7` — `ValueError(f'agents.decider.context must be a string (got {type(_ctx_raw).__name__})')`
- `load_config#8` — `ValueError(f"agents.decider.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {_key!r}")`
- `load_config#11` — `ValueError(f'agents.watcher.context must be a string (got {type(_ctx_raw).__name__})')`
- `load_config._w_num#0` — `ValueError(f'agents.watcher.{key} must be a number (got {_raw_v!r})')`
- `load_config#14` — `ValueError(f"agents.watcher.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {_w_key!r}")`
- `load_config#15` — `ValueError(f"ports must be a mapping with 'tcp', 'udp', and/or 'icmp' keys (got: {pt_raw!r})")`
- `load_config#16` — `ValueError(f"ports.tcp must be a mapping with 'allow' and/or 'passthrough' keys (got: {tcp_raw!r}). If you meant to list TCP ports, use 'ports.tcp.allow: [.....`
- `load_config#17` — `ValueError(f'ports.tcp.allow must be a list of integers (got: {raw_tcp_allow!r})')`
- `load_config#18` — `ValueError(f'ports.tcp.passthrough must be a list of integers (got: {raw_tcp_pass!r})')`
- `load_config#19` — `ValueError(f"ports.udp must be a mapping with an 'allow' key (got: {udp_raw!r}). If you meant to list UDP ports, use 'ports.udp.allow: [...]'")`
- `load_config#20` — `ValueError(f'ports.udp.allow must be a list of integers (got: {raw_udp_allow!r})')`
- `load_config#21` — `ValueError(f"ports.icmp must be a mapping with an 'allow' boolean (got: {icmp_raw!r}). To allow outbound ping, use 'ports.icmp.allow: true'")`
- `load_config#22` — `ValueError(f'ports.icmp.allow must be a boolean (got: {raw_icmp_allow!r})')`
- `_validate_agent_max_tokens#1` — `ValueError(f'{path} must be at least {_AGENT_MAX_TOKENS_FLOOR} (got {value}) — a reasoning model spends thinking tokens inside this budget before emitting th...`
- `validate_config#0` — `ValueError("'name' is required in config")`
- `validate_config#1` — `ValueError(f"'name' must be 1-63 lowercase alphanumeric characters or hyphens, starting with a letter or digit (got: {config.name!r})")`
- `validate_config#2` — `ValueError('container.image is required in config')`
- `validate_config#3` — `ValueError(f'invalid container image reference: {config.container.image!r}')`
- `validate_config#4` — `ValueError(f"isolation must be 'container', 'vm', or 'apple-container' (got: {config.isolation!r})")`
- `validate_config#5` — `ValueError(f'lifecycle must be one of {_VALID_LIFECYCLES} (got: {config.lifecycle!r})')`
- `validate_config#6` — `ValueError('container isolation is not available on macOS; use vm or apple-container instead')`
- `validate_config#7` — `ValueError(f'apple-container isolation requires macOS; current platform is {platform.system()}')`
- `validate_config#8` — `ValueError(f'apple-container isolation requires Apple Silicon (arm64); current arch is {platform.machine()}')`
- `validate_config#9` — `ValueError('vm.vcpus must be >= 1')`
- `validate_config#10` — `ValueError('vm.mem_mb must be >= 128')`
- `validate_config#11` — `ValueError(f'logging.level must be one of {_VALID_LEVELS} (got: {config.logging.level!r})')`
- `validate_config#12` — `ValueError(f'logging.{svc} must be one of {_VALID_LEVELS} or empty (got: {val!r})')`
- `validate_config#13` — `ValueError(f'invalid port spec {port_spec!r}: expected HOST_PORT:CONTAINER_PORT or BIND:HOST_PORT:CONTAINER_PORT')`
- `validate_config#14` — `ValueError(f'invalid port number {ps!r} in port spec {port_spec!r}')`
- `validate_config#15` — `ValueError(f'port {pn} out of range (1-65535) in port spec {port_spec!r}')`
- `validate_config._check_port_entry#0` — `ValueError(f'{field_label} entries must be integers (got: {entry!r})')`
- `validate_config._check_port_entry#1` — `ValueError(f'{field_label} entry {entry} out of range (1-65535)')`
- `validate_config._validate_port_list#0` — `ValueError(f'{field_label} entry {entry} appears more than once')`
- `validate_config#16` — `ValueError(f'ports.tcp.allow entry {entry} is reserved by mitmdump (8080 = HTTP-proxy listener, 8443 = transparent listener); redirecting it would loop or br...`
- `validate_config#17` — `ValueError(f'ports.tcp.allow entry {relay_port} collides with protocol_relays[{relay.name!r}].listen={relay.listen!r}; the REDIRECT would intercept connectio...`
- `validate_config#18` — `ValueError(f"ports.tcp.allow entry {container_port} collides with container.ports inbound forward {port_spec!r}; the REDIRECT would intercept connections mea...`
- `validate_config#19` — `ValueError("domains: cannot specify both 'allow' and 'block' lists")`
- `validate_config#20` — `ValueError(f"invalid domain syntax: {offenders} — expected a plain lowercase hostname (e.g. 'api.example.com', or a bare LAN name like 'fcos-vm-home-01')")`
- `validate_config#21` — `ValueError('nested_containers is not supported with vm isolation')`
- `validate_config#22` — `ValueError(f'agents.{role}.timeout_seconds must be finite and > 0')`
- `validate_config#23` — `ValueError(f"agents.decider.host {pa.host!r} must be a dotted hostname (e.g. 'agentcage.local'), not an IP literal or single label")`
- `validate_config#24` — `ValueError(f'agents.decider.host {pa.host!r} must not appear in domains.allow/block/passthrough — the control host is a synthetic, non-forwardable endpoint')`
- `validate_config#25` — `ValueError('agents.decider requires domains allowlist mode (a grant only widens an allowlist; in blocklist mode everything not blocked is already reachable).')`
- `validate_config#26` — `ValueError(f"agents.decider.provider must be 'anthropic', 'openai', or 'openrouter' (got {pa.provider!r})")`
- `validate_config#27` — `ValueError('agents.decider.model is required')`
- `validate_config#28` — `ValueError("agents.decider.api_key is required — the decider agent needs its own API key, an egress-only secret using the source: scheme (e.g. 'systemd-creds...`
- `validate_config#30` — `ValueError('agents.decider.api_key does not support cmd: sources (the egress container has no shell); use env:NAME or systemd-creds:NAME')`
- `validate_config#31` — `ValueError(f"agents.decider.api_key unknown source scheme: '{_ag_scheme}'. Valid schemes: env, systemd-creds")`
- `validate_config#32` — `ValueError(f'agents.decider.base_url must be an https:// URL (the decider API key is sent on every call; http:// would leak it in cleartext — got {pa.base_ur...`
- `validate_config#33` — `ValueError('agents.decider.rate_limit requests_per_second/burst must be >= 0')`
- `validate_config#34` — `ValueError(f'agents.decider.context is too long ({_ctx_len} chars, max 4096) — trim it or move details into a shorter summary')`
- `validate_config#36` — `ValueError("agents.watcher does not support domains blocklist mode (there the static baseline IS the block list, so the watcher's digest and its baseline rec...`
- `validate_config#37` — `ValueError(f"agents.watcher.provider must be 'anthropic', 'openai', or 'openrouter' (got {w.provider!r})")`
- `validate_config#38` — `ValueError('agents.watcher.model is required')`
- `validate_config#39` — `ValueError("agents.watcher.api_key is required — the watcher agent needs its own API key, an egress-only secret using the source: scheme (e.g. 'systemd-creds...`
- `validate_config#41` — `ValueError('agents.watcher.api_key does not support cmd: sources (the egress container has no shell); use env:NAME or systemd-creds:NAME')`
- `validate_config#42` — `ValueError(f"agents.watcher.api_key unknown source scheme: '{_w_scheme}'. Valid schemes: env, systemd-creds")`
- `validate_config#43` — `ValueError(f'agents.watcher.base_url must be an https:// URL (the watcher API key is sent on every call; http:// would leak it in cleartext — got {w.base_url...`
- `validate_config#44` — `ValueError(f'agents.watcher.interval_seconds must be >= 60 (got {w.interval_seconds}) — one LLM scan per interval, and a faster cadence would be a hot loop')`
- `validate_config#45` — `ValueError(f'agents.watcher.window_seconds must be in (0, 86400] (got {w.window_seconds})')`
- `validate_config#46` — `ValueError(f'agents.watcher.max_flows must be in [10, 2000] (got {w.max_flows})')`
- `validate_config#47` — `ValueError(f'agents.watcher.max_digest_tokens must be 0 (unbounded) or in [2000, 500000] (got {w.max_digest_tokens})')`
- `validate_config#48` — `ValueError(f'agents.watcher.context is too long ({_wctx_len} chars, max 4096) — trim it or move details into a shorter summary')`

## Not reached

- `load_config#4` — `ValueError(f'agents must be a mapping (got {type(agents_raw).__name__})')`
- `load_config#5` — `ValueError(f'agents.decider must be a mapping (got {type(dec_raw).__name__})')`
- `load_config#6` — `ValueError(f"agents.decider.enable must be a boolean (true/false) — got {type(dec_raw['enable']).__name__}")`
- `load_config#9` — `ValueError(f'agents.watcher must be a mapping (got {type(w_raw).__name__})')`
- `load_config#10` — `ValueError(f"agents.watcher.enable must be a boolean (true/false) — got {type(w_raw['enable']).__name__}")`
- `load_config#12` — `ValueError(f'agents.watcher.auto_revoke must be a boolean (true/false) — got {type(_w_ar_raw).__name__}')`
- `load_config#13` — `ValueError(f'agents.watcher.dedup_samples must be a boolean (true/false) — got {type(_w_dd_raw).__name__}')`
- `_validate_agent_max_tokens#0` — `ValueError(f'{path} must be an integer (got {type(value).__name__})')`
- `validate_config#29` — `ValueError(f"agents.decider.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {pa.api_key!r}")`
- `validate_config#35` — `ValueError('agents.decider.host must always be in never_grant (internal invariant violated)')`
- `validate_config#40` — `ValueError(f"agents.watcher.api_key must use the 'source:NAME' scheme (e.g. 'env:NAME' or 'systemd-creds:NAME') — got {w.api_key!r}")`
