ports has no inspected TCP entries (tcp.allow - tcp.passthrough is empty): transparent capture disabled, only L7 HTTP_PROXY-aware traffic will be audited
ports.tcp.allow, ports.tcp.passthrough, and ports.udp.allow are all empty: the cage will have zero outbound TCP/UDP connectivity (the proxy's filter:FORWARD policy is DROP and no ports are allowed through)
