The units, not the Lima YAML: secrets with a placeholder and a source: scheme, a relay, both agents, capture and a domain list, so the vm-local Volume= paths and the Secret= emission are recorded alongside the floor.
